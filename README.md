# EPX-CORE
A local, on-device IPC protocol with an HTTP-like `expose` / `get` / `send` programming model, automatic service discovery, and mutually-authenticated, end-to-end encrypted connections — so no other program on the machine can read or tamper with a conversation between two of your programs.
