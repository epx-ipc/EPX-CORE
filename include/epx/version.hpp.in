// Generated by CMake from version.hpp.in — do not edit directly; change
// the `project(epx VERSION ...)` line in the top-level CMakeLists.txt.
#pragma once

#define EPX_VERSION_MAJOR @PROJECT_VERSION_MAJOR@
#define EPX_VERSION_MINOR @PROJECT_VERSION_MINOR@
#define EPX_VERSION_PATCH @PROJECT_VERSION_PATCH@
#define EPX_VERSION_STRING "@PROJECT_VERSION@"

// The EPX wire protocol version *range* this build speaks (docs/SPEC.md
// section 6.2) — independent of the library's own release version above.
// Both sides of a handshake negotiate the highest mutually supported
// version; a peer whose range doesn't overlap ours fails the handshake.
// Support-window policy: a given release supports the current protocol
// version and the previous one. These are the single source of truth;
// src/protocol.hpp defines wire::kProtocolVersionMin/Max from them rather
// than hard-coding second copies.
#define EPX_PROTOCOL_VERSION_MIN 1
#define EPX_PROTOCOL_VERSION_MAX 2
// Legacy alias (pre-negotiation code referred to a single version).
#define EPX_PROTOCOL_VERSION EPX_PROTOCOL_VERSION_MAX

namespace epx {
// The library release version (this file), e.g. for a program to log
// "built against EPX 1.0.0" — distinct from the wire protocol version
// above, which changes far less often.
constexpr const char* kVersionString = EPX_VERSION_STRING;
} // namespace epx
